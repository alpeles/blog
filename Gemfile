source "https://rubygems.org"

# Use GitHub Pages dependency bundle to match production build
gem "github-pages", group: :jekyll_plugins

# (Optional) Add other whitelisted plugins here if needed later
